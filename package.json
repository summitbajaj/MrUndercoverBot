{
  "devDependencies": {
    "@types/node": "^20.14.2",
    "gts": "^5.3.1",
    "typescript": "^5.4.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "server": "ts-node src/index.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "grammy": "^1.24.1",
    "ts-node": "^10.9.2"
  }
}
